---
import logo from '../imagenes/logo.jpg';
import fondo from '../imagenes/sonido.jpg';
 
const WPP_NUMBER = import.meta.env.PUBLIC_WPP_NUMBER;
---

<section class="relative flex flex-col items-center justify-center min-h-screen text-center text-white bg-gray-900 px-4 sm:px-6 lg:px-8 overflow-hidden transition-all duration-700 animate-fade-in">
  <!-- Fondo con imagen -->
  <div class="absolute inset-0 z-0">
    <img src={fondo.src} alt="Fondo de evento" class="w-full h-full object-cover opacity-70" />
    <div class="absolute inset-0 bg-gradient-to-t from-gray-900/70 via-gray-900/40 to-transparent"></div>
  </div>

  <div class="relative z-10 max-w-4xl flex flex-col items-center">
    <img src={logo.src} alt="Sistemas Anich Logo" class="h-24 w-24 mb-6 rounded-full object-cover shadow-lg transition-transform duration-700 hover:scale-110" />
    <h1 class="text-4xl sm:text-6xl md:text-7xl font-extrabold font-poppins tracking-tight leading-tight mb-4 transition-all duration-700 animate-fade-in neon-text">
      Sistemas Anich
    </h1>
    <p class="text-lg sm:text-2xl md:text-3xl font-bold font-inter text-neon-blue mb-4 transition-opacity duration-700 animate-fade-in">
      Alquiler de Sonido, DJ y Animador para tus eventos
    </p>
    <p class="text-md sm:text-lg font-inter text-neon-pink max-w-2xl transition-opacity duration-700 animate-fade-in">
      Somos una empresa dedicada al alquiler de sonido e iluminación profesional, hacemos que tu fiesta sea única.
    </p>
  </div>
</section>
<style>
@keyframes fade-in {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
.animate-fade-in {
  animation: fade-in 1s ease-out;
}
.neon-text {
  color: #fff;
  text-shadow:
    0 0 8px #0ff,
    0 0 16px #0ff,
    0 0 32px #0ff,
    0 0 64px #0ff;
}
.text-neon-blue {
  color: #00eaff;
  text-shadow:
    0 0 6px #00eaff,
    0 0 12px #00eaff;
}
.text-neon-pink {
  color: #ff3ec8;
  text-shadow:
    0 0 6px #ff3ec8,
    0 0 12px #ff3ec8;
}
</style>
</section>